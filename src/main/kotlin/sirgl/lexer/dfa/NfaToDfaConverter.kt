package sirgl.lexer.dfa

import sirgl.lexer.nfa.regex.NfaNode

class NfaToDfaConverter {
    fun convert(entrance: NfaNode) {
        // here should be iteration over all unicode codepoints
        for (codePoint in 0 until 128) {

        }
    }
}